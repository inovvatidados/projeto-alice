# Banco de dados
pyodbc==5.2.0                     # Para conectar ao SQL Server
sqlalchemy==2.0.41                # ORM e conexão com bancos relacionais
clickhouse-connect==0.8.17        # Driver para conectar e inserir dados no ClickHouse

# Manipulação de dados
pandas==2.3.0                     # Para manipulação e transformação de dados

# IA e NLP
openai==1.88.0                    # SDK oficial da OpenAI
langchain==0.3.25                 # Framework para construir chatbots e assistentes com LLMs

# Utilitários
python-dotenv==1.1.0              # Para carregar variáveis de ambiente do arquivo .env
tqdm==4.67.1                      # Barra de progresso para scripts longos

# Desenvolvimento / Testes
jupyter==1.1.1                    # Para notebooks exploratórios
pytest==8.4.0                     # Para testes automatizados (se desejar implementar)

# Outros (opcional)
requests==2.32.4                  # Caso precise consumir APIs REST (por exemplo, em futuras features)